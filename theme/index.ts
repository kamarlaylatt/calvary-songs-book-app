import {MD3LightTheme as DefaultTheme} from 'react-native-paper';
import lightColors from "@/theme/light-colors";
import darkColors from "@/theme/dark-colors";

const theme = {
    ...DefaultTheme,
    colors: lightColors
};

export default theme;