import darkColors from "@/theme/dark-colors";
import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    colors: darkColors
};

export default theme;